-- This script creates a table in the current database in MySQL server
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
)
-- Note: VARCHAR = variable length string (size of string)